diff --git a/product/kernel/drivers/gpu/arm/midgard/hw_access/mali_kbase_hw_access.h b/product/kernel/drivers/gpu/arm/midgard/hw_access/mali_kbase_hw_access.h
index c391680..391d307 100644
--- a/product/kernel/drivers/gpu/arm/midgard/hw_access/mali_kbase_hw_access.h
+++ b/product/kernel/drivers/gpu/arm/midgard/hw_access/mali_kbase_hw_access.h
@@ -221,8 +221,8 @@ void kbase_regmap_term(struct kbase_device *kbdev);
  */
 #define kbase_reg_poll32_timeout(kbdev, reg_enum, val, cond, delay_us, timeout_us,       \
 				 delay_before_read)                                      \
-	mali_read_poll_timeout_atomic(kbase_reg_read32, val, cond, delay_us, timeout_us, \
-				      delay_before_read, kbdev, reg_enum)
+	read_poll_timeout_atomic(kbase_reg_read32, val, cond, delay_us, timeout_us, \
+				 delay_before_read, kbdev, reg_enum)
 
 /**
  * kbase_reg_poll64_timeout - Poll a 64 bit register with timeout
@@ -238,6 +238,6 @@ void kbase_regmap_term(struct kbase_device *kbdev);
  */
 #define kbase_reg_poll64_timeout(kbdev, reg_enum, val, cond, delay_us, timeout_us,       \
 				 delay_before_read)                                      \
-	mali_read_poll_timeout_atomic(kbase_reg_read64, val, cond, delay_us, timeout_us, \
-				      delay_before_read, kbdev, reg_enum)
+	read_poll_timeout_atomic(kbase_reg_read64, val, cond, delay_us, timeout_us, \
+				 delay_before_read, kbdev, reg_enum)
 #endif /* _MALI_KBASE_HW_ACCESS_H_ */
diff --git a/product/kernel/drivers/gpu/arm/midgard/mali_kbase_mem_linux.c b/product/kernel/drivers/gpu/arm/midgard/mali_kbase_mem_linux.c
index ea77f47..df0d216 100644
--- a/product/kernel/drivers/gpu/arm/midgard/mali_kbase_mem_linux.c
+++ b/product/kernel/drivers/gpu/arm/midgard/mali_kbase_mem_linux.c
@@ -2553,7 +2553,7 @@ static int kbase_cpu_mmap(struct kbase_context *kctx, struct kbase_va_region *re
 	 * See MIDBASE-1057
 	 */
 
-	__vm_flags_mod(vma, VM_DONTCOPY | VM_DONTDUMP | VM_DONTEXPAND | VM_IO, 0);
+	vm_flags_set(vma, VM_DONTCOPY | VM_DONTDUMP | VM_DONTEXPAND | VM_IO);
 
 	/*
 	 * For native struct-page-backed GPU allocations, strip VM_IO and
@@ -2566,7 +2566,8 @@ static int kbase_cpu_mmap(struct kbase_context *kctx, struct kbase_va_region *re
 	 * VM_NOHUGEPAGE prevents THP promotion races during compaction.
 	 */
 	if (reg->cpu_alloc->type == KBASE_MEM_TYPE_NATIVE) {
-		__vm_flags_mod(vma, VM_NOHUGEPAGE, VM_IO | VM_PFNMAP);
+		vm_flags_set(vma, VM_NOHUGEPAGE);
+		vm_flags_clear(vma, VM_IO | VM_PFNMAP);
 	}
 
 	vma->vm_ops = &kbase_vm_ops;
@@ -2595,11 +2596,11 @@ static int kbase_cpu_mmap(struct kbase_context *kctx, struct kbase_va_region *re
 	}
 
 	if (!kaddr)
-		__vm_flags_mod(vma, VM_PFNMAP, 0);
+		vm_flags_set(vma, VM_PFNMAP);
 	else {
 		WARN_ON(aligned_offset);
 		/* MIXEDMAP so we can vfree the kaddr early and not track it after map time */
-		__vm_flags_mod(vma, VM_MIXEDMAP, 0);
+		vm_flags_set(vma, VM_MIXEDMAP);
 		/* vmalloc remaping is easy... */
 		err = remap_vmalloc_range(vma, kaddr, 0);
 		WARN_ON(err);
@@ -3352,8 +3353,8 @@ static int kbase_tracking_page_setup(struct kbase_context *kctx, struct vm_area_
 		return -EINVAL;
 
 	/* no real access */
-	__vm_flags_mod(vma, VM_DONTCOPY | VM_DONTEXPAND | VM_DONTDUMP | VM_IO,
-		       VM_READ | VM_MAYREAD | VM_WRITE | VM_MAYWRITE | VM_EXEC | VM_MAYEXEC);
+	vm_flags_set(vma, VM_DONTCOPY | VM_DONTEXPAND | VM_DONTDUMP | VM_IO);
+	vm_flags_clear(vma, VM_READ | VM_MAYREAD | VM_WRITE | VM_MAYWRITE | VM_EXEC | VM_MAYEXEC);
 
 	return 0;
 }
@@ -3625,13 +3626,13 @@ static int kbase_csf_cpu_mmap_user_io_pages(struct kbase_context *kctx, struct v
 	if (err)
 		goto map_failed;
 
-	__vm_flags_mod(vma, VM_DONTCOPY | VM_DONTDUMP | VM_DONTEXPAND | VM_IO, 0);
+	vm_flags_set(vma, VM_DONTCOPY | VM_DONTDUMP | VM_DONTEXPAND | VM_IO);
 	/* TODO use VM_MIXEDMAP, since it is more appropriate as both types of
 	 * memory with and without "struct page" backing are being inserted here.
 	 * Hw Doorbell pages comes from the device register area so kernel does
 	 * not use "struct page" for them.
 	 */
-	__vm_flags_mod(vma, VM_PFNMAP, 0);
+	vm_flags_set(vma, VM_PFNMAP);
 
 	vma->vm_ops = &kbase_csf_user_io_pages_vm_ops;
 	vma->vm_private_data = queue;
diff --git a/product/kernel/drivers/gpu/arm/midgard/mali_kbase_native_mgm.c b/product/kernel/drivers/gpu/arm/midgard/mali_kbase_native_mgm.c
index d688509..0a7dfb3 100644
--- a/product/kernel/drivers/gpu/arm/midgard/mali_kbase_native_mgm.c
+++ b/product/kernel/drivers/gpu/arm/midgard/mali_kbase_native_mgm.c
@@ -122,8 +122,14 @@ static vm_fault_t kbase_native_mgm_vmf_insert_pfn_prot(struct memory_group_manag
 }
 
 static u64 kbase_native_mgm_update_gpu_pte(struct memory_group_manager_device *mgm_dev,
-					   unsigned int group_id, int mmu_level, u64 pte)
+					   unsigned int group_id, unsigned int pbha_id,
+					   unsigned int pte_flags, int mmu_level, u64 pte)
 {
+	CSTD_UNUSED(mgm_dev);
+	CSTD_UNUSED(pbha_id);
+	CSTD_UNUSED(pte_flags);
+	CSTD_UNUSED(mmu_level);
+
 	if (WARN_ON(group_id >= MEMORY_GROUP_MANAGER_NR_GROUPS))
 		return pte;
 
diff --git a/product/kernel/drivers/gpu/arm/midgard/mmu/mali_kbase_mmu.c b/product/kernel/drivers/gpu/arm/midgard/mmu/mali_kbase_mmu.c
index f7ca481..13d0fbe 100644
--- a/product/kernel/drivers/gpu/arm/midgard/mmu/mali_kbase_mmu.c
+++ b/product/kernel/drivers/gpu/arm/midgard/mmu/mali_kbase_mmu.c
@@ -2217,7 +2217,7 @@ static int update_parent_pgds(struct kbase_device *kbdev, struct kbase_mmu_table
 
 		kbdev->mmu_mode->entry_set_pte(&pte, target_pgd);
 		parent_page_va[parent_vpfn] = kbdev->mgm_dev->ops.mgm_update_gpu_pte(
-			kbdev->mgm_dev, MGM_DEFAULT_PTE_GROUP, parent_index, pte);
+			kbdev->mgm_dev, MGM_DEFAULT_PTE_GROUP, 0, 0, parent_index, pte);
 		kbdev->mmu_mode->set_num_valid_entries(parent_page_va, current_valid_entries + 1);
 		kunmap_pgd(parent_page, parent_page_va);
 
@@ -2679,8 +2679,8 @@ u64 kbase_mmu_create_ate(struct kbase_device *const kbdev, struct tagged_addr co
 	u64 entry;
 
 	kbdev->mmu_mode->entry_set_ate(&entry, phy, flags, level);
-	return kbdev->mgm_dev->ops.mgm_update_gpu_pte(kbdev->mgm_dev, (unsigned int)group_id, level,
-						      entry);
+	return kbdev->mgm_dev->ops.mgm_update_gpu_pte(kbdev->mgm_dev, (unsigned int)group_id, 0, 0,
+						      level, entry);
 }
 
 static int mmu_insert_pages_no_flush(struct kbase_device *kbdev, struct kbase_mmu_table *mmut,
@@ -3800,7 +3800,7 @@ static void mmu_undo_migrate_pgd_sub_page(struct kbase_mmu_table *mmut, phys_add
 	kbdev->mmu_mode->entry_set_pte(&managed_pte, old_pgd_phys);
 	target = &parent_pgd_page[index];
 	*target = kbdev->mgm_dev->ops.mgm_update_gpu_pte(kbdev->mgm_dev, MGM_DEFAULT_PTE_GROUP,
-							 level, managed_pte);
+							 0, 0, level, managed_pte);
 
 	kbdev->mmu_mode->set_num_valid_entries(parent_pgd_page, num_of_valid_entries);
 	kunmap_atomic_pgd(parent_pgd_page);
@@ -3981,7 +3981,7 @@ static int mmu_migrate_pgd_sub_page(struct kbase_mmu_table *mmut, phys_addr_t ol
 #endif
 	kbdev->mmu_mode->entry_set_pte(&managed_pte, new_pgd_phys);
 	*target = kbdev->mgm_dev->ops.mgm_update_gpu_pte(kbdev->mgm_dev, MGM_DEFAULT_PTE_GROUP,
-							 level, managed_pte);
+							 0, 0, level, managed_pte);
 
 	kbdev->mmu_mode->set_num_valid_entries(parent_pgd_page, num_of_valid_entries);
 
diff --git a/product/kernel/drivers/gpu/arm/midgard/thirdparty/mali_kbase_mmap.c b/product/kernel/drivers/gpu/arm/midgard/thirdparty/mali_kbase_mmap.c
index dc158fb..ccc6c9b 100644
--- a/product/kernel/drivers/gpu/arm/midgard/thirdparty/mali_kbase_mmap.c
+++ b/product/kernel/drivers/gpu/arm/midgard/thirdparty/mali_kbase_mmap.c
@@ -507,9 +507,12 @@ unsigned long kbase_context_get_unmapped_area(struct kbase_context *const kctx,
 			is_same_4gb_page = true;
 		}
 		kbase_gpu_vm_unlock(kctx);
-	} else if (!IS_ENABLED(CONFIG_64BIT)) {
+	}
+#if !IS_ENABLED(CONFIG_64BIT)
+	else {
 		return kbase_mm_get_unmapped_area_helper(mm, kctx->filp, addr, len, pgoff, flags);
 	}
+#endif
 
 	info.flags = 0;
 	info.length = len;
